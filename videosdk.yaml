version: "1.0"
deployment:
  id: DEPLOYMENT_ID
  entry:
    path: ./main.py
  signaling_base_url: api.videosdk.live
env:
  path: ./.env
secrets:
  VIDEOSDK_AUTH_TOKEN: VIDEOSDK_AUTH_TOKEN
deploy:
  cloud: true
